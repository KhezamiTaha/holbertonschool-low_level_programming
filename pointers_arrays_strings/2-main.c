#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
    int len;

    str = "My first strlen!";
    len = _strlen(str);
    printf("%d\n", len);
    return (0);
}
int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		if (s[i] == c)
			return ((s + i));
	}
	return ((s + i));
}
(f == NULL ? "nil" : f)